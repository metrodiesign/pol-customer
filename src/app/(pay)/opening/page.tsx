import { LinkOpeningScreen } from "@/components/pay/screens/link-opening-screen";

export const metadata = { title: "กำลังเปิดรายการ | Central Payment Gateway" };

export default function PayOpeningPage() {
  return <LinkOpeningScreen />;
}
