import { LinkRedirectingScreen } from "@/components/pay/screens/link-redirecting-screen";

export const metadata = {
  title: "กำลังเชื่อมต่อผู้ให้บริการ | Central Payment Gateway",
};

export default function PayRedirectingPage() {
  return <LinkRedirectingScreen />;
}
